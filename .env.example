# LiveKit Configuration
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# Server Configuration
PORT=4000
NODE_ENV=development

# Optional: Agent name to dispatch (must match the Python agent's registration)
AGENT_NAME=local-test